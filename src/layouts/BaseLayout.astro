---
import '../styles/global.css';

interface Props {
  title: string;
  description: string;
  lang?: 'en' | 'es';
  image?: string;
  enPath?: string;
  esPath?: string;
}

const { title, description, lang = 'en', image = '/og-default.jpg', enPath = '/', esPath = '/es' } = Astro.props;
const isES = lang === 'es';
const canonical = Astro.url.href;

const navItems = isES
  ? [
      { href: '/es', label: 'Inicio' },
      { href: '/es/analisis', label: 'Análisis' },
      { href: '/es/comparativas', label: 'Comparativas' },
      { href: '/es/guias', label: 'Guías' },
    ]
  : [
      { href: '/', label: 'Home' },
      { href: '/reviews', label: 'Reviews' },
      { href: '/comparisons', label: 'Comparisons' },
      { href: '/guides', label: 'Guides' },
    ];
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />

    <!-- Hreflang for bilingual SEO -->
    <link rel="alternate" hreflang="en" href={`https://ringadvice.com${enPath}`} />
    <link rel="alternate" hreflang="es" href={`https://ringadvice.com${esPath}`} />
    <link rel="alternate" hreflang="x-default" href={`https://ringadvice.com${enPath}`} />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />
    <meta property="og:url" content={canonical} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content={isES ? 'es_ES' : 'en_US'} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={image} />

    <!-- Schema.org -->
    <script type="application/ld+json">
      {JSON.stringify({
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "RingAdvice",
        "url": "https://ringadvice.com",
        "description": "Independent reviews and comparisons of smart health rings",
        "inLanguage": [lang]
      })}
    </script>

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="sitemap" href="/sitemap-index.xml" />
  </head>
  <body>
    <div class="glow-bg"></div>

    <header class="site-header">
      <div class="container">
        <div class="site-header__inner">
          <a href={isES ? '/es' : '/'} class="site-logo">
            <div class="site-logo__icon">◎</div>
            ring<span>advice</span>
          </a>
          <nav class="site-nav">
            {navItems.map(item => (
              <a href={item.href}>{item.label}</a>
            ))}
          </nav>
          <a href={isES ? '/es' : '/'} class="cta-btn" style="padding:7px 16px;font-size:13px;">
            {isES ? 'Ver análisis →' : 'See reviews →'}
          </a>
        </div>
      </div>
    </header>

    <main style="position:relative;z-index:1;">
      <slot />
    </main>

    <footer class="site-footer">
      <div class="container">
        <div class="site-footer__inner">
          <p>© {new Date().getFullYear()} ringadvice.com · {isES ? 'Guía independiente de anillos inteligentes' : 'Independent smart ring guide'}</p>
          <div>
            <a href={isES ? '/es/privacidad' : '/privacy'}>{isES ? 'Privacidad' : 'Privacy'}</a>
            <a href={isES ? '/es/afiliados' : '/affiliate-disclosure'}>{isES ? 'Afiliados' : 'Affiliate Disclosure'}</a>
            <a href={esPath} style="margin-left:20px">🇪🇸 ES</a>
            <a href={enPath} style="margin-left:8px">🇺🇸 EN</a>
          </div>
        </div>
        <p class="affiliate-note" style="margin-top:16px">
          {isES
            ? 'ringadvice.com participa en el Programa de Afiliados de Amazon. Ganamos una pequeña comisión cuando compras a través de nuestros enlaces, sin coste adicional para ti. Nuestras recomendaciones son siempre independientes.'
            : 'ringadvice.com is a participant in the Amazon Associates Program. We earn a small commission when you purchase through our links, at no extra cost to you. Our reviews are always independent.'}
        </p>
      </div>
    </footer>
  </body>
</html>
